import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const Signin = (props) => {
    return <Fragment>sign in page</Fragment>;
};

Signin.propTypes = {};

export default Signin;
