import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const EditProfile = (props) => {
    return <Fragment>Edit profile</Fragment>;
};

EditProfile.propTypes = {};

export default EditProfile;
