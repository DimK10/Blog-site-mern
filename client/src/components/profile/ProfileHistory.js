import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const ProfileHistory = (props) => {
    return <Fragment>Profile history</Fragment>;
};

ProfileHistory.propTypes = {};

export default ProfileHistory;
